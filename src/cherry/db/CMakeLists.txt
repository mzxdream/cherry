AUX_SOURCE_DIRECTORY(. DB_SRC_LIST)
AUX_SOURCE_DIRECTORY(${PROJECT_SOURCE_DIR}/util UTIL_SRC_LIST)
INCLUDE_DIRECTORIES(
    ${PROJECT_SOURCE_DIR}/util
    )
SET(LIBDB_SRC
    ${DB_SRC_LIST}
    ${UTIL_SRC_LIST})
ADD_LIBRARY(db SHARED ${LIBDB_SRC})

